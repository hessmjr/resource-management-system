version: '3'
services:
    web:
        build:
            context: ../
            dockerfile: docker/Dockerfile
        ports:
            - "5000:5000"
    mysql:
        build:
            context: ../
            dockerfile: docker/Dockerfile-MySQL
        ports:
            - "3306:3306"
