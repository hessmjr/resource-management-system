version: '3'
services:
    web:
        build: .
        ports:
            - "80:80"
    mysql:
        build:
            context: ../
            dockerfile: dockerfiles/Dockerfile-MySQL
        ports:
            - "3306:3306"
